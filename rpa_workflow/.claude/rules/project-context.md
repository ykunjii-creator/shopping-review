<!-- discovery-metadata: cs=0 xaml=6 deps=5 -->
# ReviewDefectRPA — Project Context

> Auto-generated by project discovery agent. Regenerate after significant project changes.

## Overview

| Property | Value |
|----------|-------|
| **Name** | ReviewDefectRPA |
| **Type** | Process |
| **Description** | 올리브영 리뷰 수집 → AI 분석 서버 호출 → 엑셀 기록 → 긴급 메일 발송 RPA (PRD §5) |
| **Target Framework** | Windows |
| **Expression Language** | VisualBasic |

## Dependencies

| Package | Version | Category | Description |
|---------|---------|----------|-------------|
| UiPath.System.Activities | 23.10.3 | Core | 기본 시스템 액티비티 |
| UiPath.UIAutomation.Activities | 23.10.5 | UI Automation | 브라우저 자동화 (올리브영 스크래핑) |
| UiPath.Excel.Activities | 2.23.4 | Excel | Master_Log 누적 및 리포트 생성 |
| UiPath.Mail.Activities | 1.23.4 | Mail | Outlook 긴급 메일 발송 |
| UiPath.WebAPI.Activities | 1.16.2 | Web | HTTP POST /analyze-reviews |

## Project Structure

```
rpa_workflow/
├── Main.xaml                        # 진입점 오케스트레이터
├── Scrape_Reviews.xaml              # 올리브영 브라우저 스크래핑
├── Process_Results.xaml             # JSON 파싱 + 엑셀 누적
├── Send_Urgent_Mail.xaml            # 긴급 Outlook 메일 발송
├── Generate_Summary_Report.xaml     # 종합 리포트 4시트 생성
├── Verify_RPA_Results.xaml          # 실행 결과 검증
├── Data/
│   ├── Master_Log.xlsx              # 누적 분석 로그 (런타임 생성)
│   └── Reports/                     # 배치별 리포트 저장 디렉토리
├── Logs/
│   └── execution_log.txt            # 검증 로그 (런타임 생성)
└── project.json
```

| File Type | Count |
|-----------|-------|
| RPA workflows (.xaml) | 6 |
| Coded workflows (.cs) | 0 |
| Source files (helpers/models) | 0 |

## Entry Points

| File | Input Arguments | Output Arguments | Purpose |
|------|----------------|------------------|---------|
| Main.xaml | `in_GoodsUrl: String` (필수, 기본값: 올리브영 상품 URL 템플릿) | none | 전체 파이프라인 오케스트레이션 |

## Conventions

- **File naming**: PascalCase + 언더스코어 구분 (예: `Scrape_Reviews.xaml`)
- **Variable naming**: `str` (String), `int` (Int32), `bool` (Boolean), `dt` (DataTable), `jobj` (JObject), `jarr` (JArray), `lst` (List) 접두사 헝가리안 표기
- **Argument naming**: `in_` (입력), `out_` (출력) 접두사
- **Error handling**: `RetryScope`로 재시도(3회), `Throw`로 Retry 유발, 오류 누적은 `lstErrors: List(Of String)` 패턴
- **Logging**: `ui:LogMessage` Level=Info 단계별 로그, Level=Trace 세부 주석, `AppendLine`으로 파일 로그

## Key Workflows

| Workflow | Purpose | Services/Activities Used |
|----------|---------|--------------------------|
| Main.xaml | 전체 파이프라인 조율 (수집→분석→처리→메일→리포트→검증) | LogMessage, HttpClient, InvokeWorkflowFile, If, Assign |
| Scrape_Reviews.xaml | 올리브영 상품 페이지 브라우저 자동화 → `dtReviews(4컬럼)` 반환 | NApplicationCard, NClick, ExtractData, RetryScope, BuildDataTable |
| Process_Results.xaml | AI 응답 JSON 파싱 → `dtResults(12컬럼)` 빌드 → Master_Log.xlsx 누적 + 조건부 하이라이트 | DeserializeJson, BuildDataTable, ForEach, ExcelApplicationScope, WriteRange, AppendRange, SetRangeColor |
| Send_Urgent_Mail.xaml | 제품결함 필터 → 긴급 xlsx 생성 → Outlook 발송 (3회 재시도) | ExcelApplicationScope, WriteRange, OutlookMailMessage, RetryScope |
| Generate_Summary_Report.xaml | summary_analysis JSON → 종합 리포트 4시트 xlsx 생성 | DeserializeJson, ExcelApplicationScope, WriteRange |
| Verify_RPA_Results.xaml | 파일 존재·12컬럼·처리건수·빈셀·메일발송 검증 → PASS/FAIL 반환 | PathExists, ReadRange, AppendLine, LogMessage |

## Architecture

- **Pattern**: Main 오케스트레이터가 단계별 워크플로를 순차 InvokeWorkflowFile로 호출
- **Data flow**: `in_GoodsUrl` → `dtReviews(4컬럼)` → HTTP POST JSON → `dtResults(12컬럼)` + `intDefectCount` + `jobjSummary` → Excel 누적 + 조건부 메일 + 종합 리포트
- **Execution flow**: Scrape_Reviews → (0건이면 안전 종료) → HTTP POST localhost:8000/analyze-reviews → Process_Results → (결함 > 0이면 Send_Urgent_Mail) → Generate_Summary_Report → Verify_RPA_Results
- **External integrations**: AI 분석 서버 `http://localhost:8000/analyze-reviews` (HTTP POST JSON), Outlook (Windows), 올리브영 웹사이트 (브라우저 자동화)

## Quick Reference

- **Run**: `uip rpa run --file-path "Main.xaml" --project-dir "/Users/koseonje/dev/shopping-review/rpa_workflow"`
- **Validate**: `uip rpa validate --file-path "<file>" --project-dir "/Users/koseonje/dev/shopping-review/rpa_workflow" --output json`
- **Build**: `uip rpa build "/Users/koseonje/dev/shopping-review/rpa_workflow" --output json`
- **Key files to read first**: `Main.xaml`, `Process_Results.xaml`, `Scrape_Reviews.xaml`
